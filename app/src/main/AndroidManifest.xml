<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gugu.wegolsu.k_seastem">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.DIAL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CatchBanActivity" />
        <activity android:name=".EEZActivity" />
        <activity android:name=".TACActivity" />
        <activity android:name=".PirateActivity" />
        <activity android:name=".GlobalFishActivity" />
        <activity android:name=".TotalYearActivity" />
        <activity android:name=".NewsActivity" />
        <activity android:name=".CallActivity"></activity>
    </application>

</manifest>